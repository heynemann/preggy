language: python
python:
  - "2.6"
  - "2.7"
  - "3.2"
  - "3.3"
  - "pypy"

install:
    # install python requirements
    - pip install -r requirements.txt --use-mirrors

script:
    # finally run tests
    - make ci-test
